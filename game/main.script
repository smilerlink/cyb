local game = require 'game.main'

local function on_show_menu()

end

local function on_show_game()

end

function init(self)
	if game.is_connected() then
		on_show_menu()
	else
		game.on_show_menu(on_show_menu)
		game.on_show_game(on_show_game)
		game.connect()
	end
end
